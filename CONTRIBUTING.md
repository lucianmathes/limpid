# Contributing to LIMPID

Thank you for your interest in helping improve LIMPID!
Whether you want to fix a bug, or suggest new features, this guide
will help you get started.

## Development Setup
Clone the repository then inside the directory run
```
pip install --upgrade pip
pip install -r requirements.txt
pip install --editable .
```
